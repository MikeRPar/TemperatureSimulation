Project Specific:
Add dear Imgui for UI
Cube class to fill vertex buffers and such


Engine specific:

Physical Device Selection:
Consider all options and score for device selection.
Add support for using multiple GPUs?

Swap chain:
Post processing?
Improve recreation

Image view swizzle?

Create shaders in runtime?

Use instancing for cubes/objects?

Different input assembly?

Different rasterizer settings for rendering? allow the user the change them?

push constants for dynamic shader info

Set for 2D or 3D modes

Codebase:
Change the structure of both headers and source for better orginization

note to self:
look into understanding suitable memory buffers better
Do challenge for transfer queue, see staging buffer section from vulkan tutorial

custom allocator for buffers? see staging and index buffer chapters